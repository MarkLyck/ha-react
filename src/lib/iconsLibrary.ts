import { library } from '@fortawesome/fontawesome-svg-core'

// Light
import {
  faAngleUp,
  faAngleDown,
  faDroplet,
  faTemperatureHalf,
} from '@fortawesome/pro-light-svg-icons'

library.add(
  // Light
  faAngleUp,
  faAngleDown,
  faDroplet,
  faTemperatureHalf
)
